const styles =theme =>({
    taskboard: {
        // display: "flex",
        alignItems: "center"
    },
    shape: {
        backgroundColor: theme.color.primary,
        fontSize: "20px"
    }
        
    
});
export default styles;